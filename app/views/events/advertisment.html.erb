 <% provide(:title, "Advertisements" ) %>
<%- model_class = Event -%>
<div class="page-header">
  <h1>Advertisements</h1>
  
   <h6>Advertisements are used only the main page,do not have a "page" to go to and do not show up on the calendar </h6>
   <h6>Maximum of 3 "Forced on Main Page" </h6>
</div>
 		
<table class="table table-striped">
  <thead>
    <tr>
 
      <th>Name:</th>
      <th>Forced on Main Page:</th>
      <th>Ad Enabled:</th>
    <th>Image</th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |event| %>
      <tr>
  
        <td><%= event.event_name %></td>
        <td><%= event_form_bool(event.force_on_main_page) %></td>
	 
 
         <td><%= event_form_bool(event.display_main_page) %></td>
        <td>
			<% if event.event_pictures.present? %>
			<% for event_picture in event.event_pictures %>
			<% if event_picture.image_processed? %>
			    <%= image_tag(event_picture.event_image_url(:stamp), :class =>"img-rounded" ) %>
			  <% else %>
			    <em>Processing...</em>
			  <% end %>
 
			<% end %>

		 
			<% end %>
	</td>
        <td>
			      
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_event_path(event), :class => 'btn btn-mini-flat' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      event_path(event),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-danger btn-mini-flat' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

 
</div> 