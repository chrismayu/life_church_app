<%= simple_form_for @site_setup, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                site_setups_path, :class => 'btn' %>
  </div>
<% end %>
