  		
  		
  		<p><span class="icon-caret-down "></span> <%= faq.title %> %></p>  
  		
	 
 <div>
 <p> 
	 
	 <%= faq_markdown(faq.the_answer) %> 
 
 
 </p> </div>

<% if user_signed_in? %>
						 
						  <% if current_user.has_role? :admin %>
 <%= link_to t('.edit', :default => t("helpers.links.edit")), edit_faq_path(faq), :class => 'btn
        btn-mini' %> <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
        faq_path(faq), :method => :delete, :data => { :confirm => t('.confirm', :default =>
        t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini
        btn-danger' %>
		<br>
		<% end %>
			<% end %>

 <br>
